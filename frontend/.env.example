# Leave empty when nginx proxies /api to the backend (Docker + EKS setup).
# VITE_API_URL=

# Optional: set a separate backend origin if not using nginx proxy:
# VITE_API_URL=https://your-backend.example.com
